[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stegloc"
version = "0.3.0"
description = "LSB steganography with SHA-256, RSA and Ed25519 verification"
requires-python = ">=3.11"
dependencies = [
    "cryptography==50.0.1",
    "fastapi==0.141.1",
    "numpy==2.5.3",
    "pillow==12.3.0",
    "python-multipart==0.0.32",
    "uvicorn==0.52.4",
]

[project.optional-dependencies]
desktop = [
    "pywebview==6.2.1",
]
build = [
    "pyinstaller==6.22.3",
]
dev = [
    "httpx==0.28.1",
    "pytest==9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = [
    "ignore:The anyio.abc.BlockingPortal alias is deprecated.*:DeprecationWarning:starlette.testclient",
]

[tool.setuptools.packages.find]
include = ["backend*"]
